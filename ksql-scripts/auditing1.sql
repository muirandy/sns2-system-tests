CREATE STREAM AUDIT WITH (PARTITIONS=4,REPLICAS=1)
AS SELECT TRACEID as "TRACE_ID",
ROWKEY as "ORDER_ID",
TIMESTAMPTOSTRING(ROWTIME, 'yyyy-MM-dd HH:mm:ss.SSS') as "TIMESTAMP",
'ConvertedFromXmlToJson' as "EVENT",
'' as "ASSOCIATION"
FROM INSTRUCTIONS_STREAM_1 WITH (VALUE_FORMAT='JSON');